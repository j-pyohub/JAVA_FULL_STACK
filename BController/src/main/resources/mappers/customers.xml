<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.oopsw.bcontroller.dao.CustomerDAO">
	<select id="getCustomers" resultType="java.lang.String">
		select cust_id from cust_info
	</select>
	<select id="getCustomer" parameterType="java.lang.String" resultType="com.oopsw.bcontroller.vo.CustomerVO">
		select name, point from cust_info where cust_id = #{customerId}
	</select>
</mapper>
